# Replace with the VPS public IP. Do not add a trailing slash.
APP_URL=http://203.0.113.10
APP_PORT=80

POSTGRES_DB=realtime_quiz_app
POSTGRES_USER=quiz_app
POSTGRES_PASSWORD=replace-with-a-long-random-database-password

# Must contain at least 32 characters. Generate with: openssl rand -hex 32
JWT_SECRET=replace-with-a-random-secret-at-least-32-characters-long
JWT_EXPIRES_IN=7d
