# QuizPlatform Frontend

Desktop-focused MVP frontend for real-time quizzes. The application currently
uses mock data and local component state.

## Commands

```bash
npm install
npm run dev
npm run lint
npm run build
```

The original Figma Make export is kept in `src/figma-make` as a temporary visual
reference. The running application does not import from it.
